type Attestation @entity(immutable: false) {
  id: ID!
  schema: Schema!
  replacedBy: Bytes!
  attester: Bytes!
  portal: Portal!
  attestedDate: BigInt!
  expirationDate: BigInt!
  revocationDate: BigInt!
  version: BigInt!
  revoked: Boolean!
  subject: Bytes!
  encodedSubject: Bytes!
  attestationData: Bytes!
  decodedData: [String!]
  auditInformation: AuditInformation!
}

type Module @entity(immutable: false) {
  id: ID!
  moduleAddress: Bytes!
  name: String!
  description: String!
  auditInformation: AuditInformation!
}

type Portal @entity(immutable: false) {
  id: ID!
  ownerAddress: Bytes!
  modules: [Bytes!]
  isRevocable: Boolean!
  name: String!
  description: String!
  ownerName: String!
  attestationCounter: Int
  auditInformation: AuditInformation!
}

type Schema @entity(immutable: false) {
  id: ID!
  name: String!
  description: String!
  context: String!
  schema: String!
  attestationCounter: Int
  auditInformation: AuditInformation!
}

type Counter @entity(immutable: false) {
  id: ID!
  attestations: Int
  modules: Int
  portals: Int
  schemas: Int
}

type Issuer @entity(immutable: false) {
  id: ID!
  auditInformation: AuditInformation!
}

type RegistryVersion @entity(immutable: false) {
  id: ID!
  versionNumber: Int
  timestamp: BigInt
  auditInformation: AuditInformation!
}

type AuditInformation @entity(immutable: false) {
  id: ID!
  creation: Audit!
  lastModification: Audit!
  modifications: [Audit!]!
}

type Audit @entity(immutable: false) {
  id: ID!
  blockNumber: BigInt!
  transactionHash: Bytes!
  transactionTimestamp: BigInt!
  fromAddress: Bytes!
  toAddress: Bytes
  valueTransferred: BigInt
  gasPrice: BigInt
}

type RegistryUpdate @entity(immutable: false) {
  id: ID!
  registryType: String!
  registryName: String!
  registryAddress: Bytes!
  auditInformation: AuditInformation!
}
