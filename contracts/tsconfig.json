{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../tsconfig.node.json",
  "compilerOptions": {
    "composite": true,
    "outDir": "./dist",
    "rootDir": ".",
    "types": ["node"]
  },
  "include": ["script/**/*.ts", "hardhat.config.ts"],
  "exclude": ["node_modules", "out", "cache_forge", "cache_hardhat", "artifacts", "typechain-types"]
}
